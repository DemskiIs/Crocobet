import { UserStylesDirective } from './user-styles.directive';

describe('UserStylesDirective', () => {
  it('should create an instance', () => {
    const directive = new UserStylesDirective();
    expect(directive).toBeTruthy();
  });
});
